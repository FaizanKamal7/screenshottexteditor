numpy<2.0
pillow>=11.0
opencv-contrib-python==4.10.0.84
setuptools

# Engine 1: current production PaddleOCR / PP-OCRv5 mobile — same pins as
# services/pipeline/requirements.txt.
pyclipper==1.3.0.post6
paddlepaddle==3.3.1
paddleocr==3.7.0

# Engine 2 & 3: RapidOCR, unified package — ONNX Runtime and OpenVINO
# backends both installed so the same RapidOCR() call can select either via
# engine_name at runtime (no separate images needed).
rapidocr>=2.0.6
onnxruntime>=1.19
openvino>=2024.0

# Engine 4: Tesseract 5 (apt package, installed in Dockerfile) via pytesseract.
pytesseract>=0.3.13

# Accuracy comparison (CER / edit distance). rapidfuzz (MIT) replaces the
# GPL-2.0-or-later python-Levenshtein; same unit-cost Levenshtein distance.
rapidfuzz>=3.14

# Downstream validation phase only: reuses services/pipeline's stages
# (separate/match/render_stage/fonts.registry) against a promising engine's
# output, mounted read-only at /bench/pipeline_src — same pins as
# services/pipeline/requirements.txt so behavior matches production.
skia-python==144.0.post2
scikit-image>=0.24
pydantic>=2.9
