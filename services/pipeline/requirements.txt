fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12
pydantic>=2.9
pillow>=11.0
numpy<2.0
setuptools
# 2.x's paddleocr pulled a kitchen sink of unrelated tooling (pdf2docx,
# visualdl, flask, pandas, three mismatched-vintage opencv builds) that
# produced zlib.error/segfaults inside unrelated compiled extensions
# (pyclipper, paddlepaddle, scipy) depending on load order — reproduced
# identically across Cloud Build and a separate local Docker build. 3.x's
# modular paddlex[ocr-core] extra drops all of that and pins
# opencv-contrib-python==4.10.0.84 exactly, which is what stages/detect.py
# and stages/match.py's `cv2` import now resolves to — no separate
# opencv-python/opencv-python-headless entry needed here.
# Pinned below paddleocr's default pyclipper resolve (1.4.0), whose cp312
# wheel threw a zlib import error independent of the above — cheap insurance
# since 1.3.0.post6 is a known-stable release either way.
pyclipper==1.3.0.post6
paddlepaddle==3.3.1
paddleocr==3.7.0
skia-python==144.0.post2
scikit-image>=0.24
pytest>=8.3
httpx>=0.27
