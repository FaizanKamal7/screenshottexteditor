services:
  pipeline:
    build: ./services/pipeline
    ports:
      - "8000:8000"
    environment:
      - PIPELINE_SHARED_SECRET=${PIPELINE_SHARED_SECRET:-dev-secret}
    volumes:
      - ./services/pipeline:/app
      - paddleocr-models:/root/.paddleocr
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload

volumes:
  paddleocr-models:
